echo "=================================================================="
echo "REPLAYING"

if [ $# -eq 1 ]
then
	echo "REPLAY BUG = $1"
	echo "=================================================================="
	./fuzzer --replay $1 ./pstotext-linux-x86 --fuzz-file postscript
else
	echo "=================================================================="
	./fuzzer sd
fi
echo "------------------------------------------------------------------"

